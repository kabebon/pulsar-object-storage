# Binaries
/bin/
*.exe
*.exe~
*.dll
*.so
*.dylib
/pulsar

# Templ generated sources (regenerated by `make templates`)
# We keep them committed for reviewers who don't run templ, but they're
# listed here so accidental local edits don't pollute diffs. Uncomment to ignore:
# **/*_templ.go

# Test artifacts
*.out
coverage.txt
coverage.html

# Local environment
.env
.env.local
*.local

# Editor / OS noise
.idea/
.vscode/
.DS_Store
Thumbs.db

# Dependency directories
/vendor/

# Docker volumes (if bind-mounted locally)
/data/
/pgdata/
